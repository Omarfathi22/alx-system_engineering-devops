#!/usr/bin/env bash
# Configure Nginx to run as nginx user on port 8080
apt-get update && apt-get install -y nginx
useradd -r -s /bin/false nginx || true
sed -i 's/^user .*/user nginx;/; s/listen 80;/listen 8080;/' /etc/nginx/nginx.conf
systemctl restart nginx

